FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD sh -c "uvicorn main:app --host :: --port ${PORT:-8000} --proxy-headers --forwarded-allow-ips='*' --timeout-keep-alive 65"
